<%= form_for(@cms_feedback) do |f| %>
  <% if @cms_feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cms_feedback.errors.count, "error") %> prohibited this cms.feedback from being saved:</h2>

      <ul>
      <% @cms_feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :tel %><br>
    <%= f.text_field :tel %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
